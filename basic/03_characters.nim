#[ a variable with char type would represent a single ASCII char.
this can be letters, symbols or single numbers.
it would be declared like 'z'. however, it's single so 'ab' would
throw a error.]#

let
  h = 'z'
  i = '+'
  j = '2'
# k = '35'  # error
# l = 'xy'  # error

echo h
echo i
echo j
